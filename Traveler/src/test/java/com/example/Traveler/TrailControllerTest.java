package com.example.Traveler;
/*
import com.example.Traveler.controller.TrailController;
import com.example.Traveler.model.entity.Trail;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;

import java.util.List;

@WebMvcTest(TrailController.class)
public class TrailControllerTest {

    @Autowired
    private MockMvcmockMvc;

    @MockBean
    private TrailServicetrailService;

    @Test
    public void shouldReturnTrailList() throws Exception {
        List<Trail> trails = Arrays.asList(new Trail("Trail 1"), new Trail("Trail 2"));
        given(trailService.getAllTrails()).willReturn(trails);

        mockMvc.perform(get("/api/trails"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$", hasSize(2)))
                .andExpect(jsonPath("$[0].name", is(trails.get(0).getName())));
    }
}


 */

