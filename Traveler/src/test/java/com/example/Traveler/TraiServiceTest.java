package com.example.Traveler;

import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;

/*
@SpringBootTest
public class TrailServiceTest {

    @MockBean
    private TrailRepositorytrailRepository;

    @Autowired
    private TrailServicetrailService;

    @Test
    public void whenAddTrail_thenTrailShouldBeSaved() {
        Trail trail = new Trail("Test Trail", 100.0, "Easy");
        Mockito.when(trailRepository.save(any(Trail.class))).thenReturn(trail);

        Trail created = trailService.addTrail(trail);

        assertThat(created.getName()).isSameAs(trail.getName());
    }
}

*/
