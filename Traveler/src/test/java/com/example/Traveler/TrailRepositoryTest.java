package com.example.Traveler;
/*
import com.example.Traveler.model.entity.Trail;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;

import java.util.Optional;

import static org.assertj.core.api.AssertionsForClassTypes.assertThat;

@DataJpaTest
public class TrailRepositoryTest {

    @Autowired
    private TrailRepositorytrailRepository;

    @Test
    public void whenFindById_thenReturnTrail() {
// Zapisz przykładową trasę, a następnie spróbuj ją odnaleźć
        Trail trail = new Trail("Trail Name", 150.0, "Medium");
        trailRepository.save(trail);

        Optional<Trail> found = trailRepository.findById(trail.getId());

        assertThat(found.isPresent()).isTrue();
        assertThat(found.get().getName()).isEqualTo(trail.getName());
    }
}

*/